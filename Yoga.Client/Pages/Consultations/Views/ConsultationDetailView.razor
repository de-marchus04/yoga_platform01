@if (Loading)
{
    <section class="page-hero">
        <div class="container page-hero-content">
            <div class="skeleton" style="height: 1.5rem; width: 20%; margin-bottom: 2rem;"></div>
            <div class="skeleton" style="height: 4rem; width: 60%; margin-bottom: 2rem;"></div>
        </div>
    </section>
}
else if (Item == null)
{
    <section class="page-hero">
        <div class="container page-hero-content">
            <h1>@(Loc.CurrentLang == "uk" ? "Консультацію не знайдено" : "Consultation not found")</h1>
            <a href="/consultations" class="btn btn-outline" style="margin-top:1.5rem; display:inline-flex;">@(Loc.CurrentLang == "uk" ? "До каталогу" : "Back to catalog")</a>
        </div>
    </section>
}
else
{
    <section class="page-hero">
        <div class="page-hero-bg" style="background-image: url('@(string.IsNullOrEmpty(Item.ImageUrl) ? "https://images.unsplash.com/photo-1518611012118-696072aa579a?auto=format&fit=crop&w=1600&q=80" : Api.ResolveMediaUrl(Item.ImageUrl))');"></div>
        <div class="page-hero-overlay"></div>
        <div class="container page-hero-content">
            @if (!string.IsNullOrEmpty(Item.Eyebrow))
            {
                <p class="section-eyebrow">@Item.Eyebrow</p>
            }
            <h1>@DisplayTitle</h1>
            <div class="section-divider" style="margin:2rem 0;"></div>
            @if (!string.IsNullOrEmpty(Item.Quote))
            {
                <p>@Item.Quote</p>
            }
        </div>
    </section>

    @if (!string.IsNullOrEmpty(Item.Duration) || !string.IsNullOrEmpty(Item.Format))
    {
        <section class="format-strip">
            <div class="format-strip-grid">
                @if (!string.IsNullOrEmpty(Item.Duration))
                {
                    <div class="format-strip-cell">
                        <span class="format-strip-label">@(Loc.CurrentLang == "uk" ? "Тривалість" : "Duration")</span>
                        <span class="format-strip-value">@Item.Duration</span>
                    </div>
                }
                @if (!string.IsNullOrEmpty(Item.Format))
                {
                    <div class="format-strip-cell">
                        <span class="format-strip-label">@(Loc.CurrentLang == "uk" ? "Формат" : "Format")</span>
                        <span class="format-strip-value">@Item.Format</span>
                    </div>
                }
            </div>
        </section>
    }

    <section class="overview-section">
        <div class="container">
            <div class="overview-grid">
                <div class="overview-desc reveal">
                    @if (!string.IsNullOrEmpty(Item.Description))
                    {
                        @foreach (var para in Item.Description.Split('\n', StringSplitOptions.RemoveEmptyEntries))
                        {
                            <p>@para</p>
                        }
                    }
                </div>
            </div>
        </div>
    </section>

    <section class="page-cta">
        <div class="container" style="text-align:center;">
            @if (!string.IsNullOrEmpty(Item.CtaHeading))
            {
                <h2>@Item.CtaHeading</h2>
                <p>@Item.CtaText</p>
            }
            else
            {
                <h2>@(Loc.CurrentLang == "uk" ? "Розпочати роботу" : "Start working")</h2>
            }
            <button class="btn btn-primary" @onclick="OnCtaClick">@(Loc.CurrentLang == "uk" ? "Детальніше" : (Loc.CurrentLang == "ru" ? "Подробнее" : "Read more"))</button>
        </div>
    </section>
}

@code {
    [Parameter] public ConsultationDto? Item { get; set; }
    [Parameter] public bool Loading { get; set; }
    [Parameter] public string? DisplayTitle { get; set; }
    [Parameter] public EventCallback OnCtaClick { get; set; }
    
    [Inject] public LocalizationService Loc { get; set; } = default!;
    [Inject] public ApiService Api { get; set; } = default!;
}
